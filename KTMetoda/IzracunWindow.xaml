<Window x:Class="KTMetoda.IzracunWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KTMetoda"
        mc:Ignorable="d"
        Title="IzracunWindow" Height="1080" Width="1024">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <DataGrid x:Name="DataGridUtezi" AutoGenerateColumns="False" HeadersVisibility="All" FontSize="30" HorizontalAlignment="Left" Grid.Row="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Parameter" Binding="{Binding Key}" />
                <DataGridTextColumn Header="Vrednost" Binding="{Binding Value}" />
            </DataGrid.Columns>
        </DataGrid>
        <DataGrid x:Name="myDataGrid" HeadersVisibility="All" FontSize="30" HorizontalAlignment="Center" Grid.Row="0">
            <!--<DataGrid.Columns>
                <DataGridTextColumn Header="Parametri" Binding="{Binding Path=Key}"/>
            </DataGrid.Columns>-->
            
            vse naredi z listvievom pa naredi gridl colum za seperator
        </DataGrid>
    </Grid>
</Window>
